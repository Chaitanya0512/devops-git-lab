[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "calculator-app"
version = "1.0.0"
description = "Simple Python calculator application"
requires-python = ">=3.10"
